{
  "name": "jest-store-log",
  "version": "1.1.0",
  "description": "Simple one class library to for storing the console output with jest",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch --coverage",
    "prepack": "tsc -b && suf"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "jest",
    "console.log"
  ],
  "author": "Syler",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "jest": "^26.6.3",
    "suf-cli": "^0.1.0",
    "ts-jest": "^26.4.3",
    "typescript": "^4.0.5"
  }
}
